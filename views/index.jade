extends layout

block content
  h1= title
  form#filter
    each val, key in feeds
        input(id='#{key}')(type='checkbox')(checked='checked')
        label(class='#{key}')(for='#{key}') #{val}
  ul#items
